# ⚡️ Фильтр «Скорость Света»

Асинхронный сервис для анализа «желтушности» новостных статей с сайта ИНОСМИ.РУ. Проект создан для демонстрации мощи асинхронного Python при обработке больших объемов текстовых данных.

## 🚀 Особенности

- **Асинхронность на стероидах:** Благодаря `aiohttp` и `asyncio`, сервис качает и анализирует десятки статей одновременно.
- **Умная очистка:** Используются специализированные адаптеры для извлечения только полезного текста из HTML-разметки (без рекламы и меню).
- **Лингвистический анализ:** Морфологический разбор слов через `pymorphy3` позволяет точно определять «заряженные» слова в любой форме.
- **Отказоустойчивость:** 
    - Таймауты на скачивание (5 сек) и анализ (3 сек).
    - Обработка сетевых ошибок и ошибок парсинга.
    - Ограничение нагрузки (максимум 10 URL за один запрос).
- **Готов к API:** Возвращает результаты в чистом JSON-формате.

## 🛠 Требования

- Python 3.11+
- Зависимости из `requirements.txt` (включая `pymorphy3` для совместимости с новыми версиями Python).

## 📥 Установка

1. Клонируйте репозиторий.
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

## 🖥 Использование

### Запуск веб-сервера
Сервер загружает словари в память один раз при старте, что обеспечивает мгновенную обработку последующих запросов.
```bash
python3 server.py
```
Сервер будет доступен по адресу: `http://localhost:8080`

**Пример запроса:**
```bash
curl "http://localhost:8080/?urls=https://inosmi.ru/20190629/245384784.html,https://inosmi.ru/20240206/shveciya-267746142.html"
```

**Пример ответа:**
```json
[
  {
    "url": "https://inosmi.ru/20190629/245384784.html",
    "score": 0.56,
    "words_count": 732,
    "status": "OK"
  },
  {
    "url": "https://inosmi.ru/20240206/shveciya-267746142.html",
    "score": null,
    "words_count": null,
    "status": "FETCH_ERROR"
  }
]
```

### Консольный скрипт (для быстрых тестов)
```bash
python3 main.py https://inosmi.ru/20190629/245384784.html
```

## 🧪 Тестирование
Для запуска тестов (включая проверку асинхронных функций и таймаутов) выполните:
```bash
pytest
```

## 🧠 Как это работает

Проект решает классическую проблему баланса между сетевыми задержками (**I/O-bound**) и вычислительной нагрузкой (**CPU-bound**):
1. **Сетевая часть:** Запросы выполняются параллельно через `asyncio.gather`.
2. **Вычислительная часть:** Тяжелый морфологический анализ выносится в отдельные потоки через `run_in_executor`, чтобы не блокировать основной цикл сервера (Event Loop). Это позволяет серверу оставаться отзывчивым даже при обработке огромных текстов.

---
Проект выполнен в рамках учебного курса для разработчиков.